# fastapi>=0.109.0's pinned starlette still passed TestClient an `app=`
# kwarg httpx>=0.28 removed from Client.__init__, breaking every test
# using TestClient with an unpinned/modern httpx (TypeError at client
# construction, not an assertion failure) -- verified working with
# fastapi 0.141.1 specifically; bumped the floor accordingly.
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
kafka-python>=2.0.2
httpx>=0.26.0
pytest>=7.0.0
